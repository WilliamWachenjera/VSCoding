#include <iostream>

using namespace std;

int main()
{
    int x , y = 10;
    x = (y < 10) ? 30 : 40; //if y < 10, x == 30, else x == 40 
    cout << "value of x: "<<x << endl;

    return 0;
} // namespace std;
